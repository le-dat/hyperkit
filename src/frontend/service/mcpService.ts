const mcpService = {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  getRecommendations: async (_data: any, _options?: any) => {
    return { recommendations: [], data: [] };
  },
  // Add other methods as needed
};

export default mcpService;
